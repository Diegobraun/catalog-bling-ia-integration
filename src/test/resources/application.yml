spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  config:
    import: ""

ia:
  provider: gemini
  pesquisa-provider: gemini

gemini:
  api-key: test-key

bling:
  client-id: test-id
  client-secret: test-secret

imagem:
  headless:
    enabled: false
